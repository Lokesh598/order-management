server:
  port: 8085
spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Lokesh598/order-management-spring-cloud-config
          clone-on-start: true